import types

mqtt = types.SimpleNamespace()
mqtt.host = "MQTT_host"
mqtt.username = "etc"
mqtt.password = "pwd"

cam = types.SimpleNamespace()
cam.host = "Hikvision_Camera_host"
cam.username = "admin"
cam.password = "12345" # This isn't the default for new Hikvision cameras anymore

